[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "redops"
version = "1.0.0"
description = "Modular Cybersecurity Tooling with AI-Driven Summarization"
authors = [{name = "AreteDriver"}]
license = {file = "LICENSE"}
dependencies = [
    "openai>=1.0.0",
    "pytest>=7.0.0"
]
